function calculaForcaDaPassword(string) {
if (string === "") {
return "Por favor introduza uma password válida.";
}

let pontos = 0;

if (string.length >= 8) {
pontos++;
}

if (string.match(/\d+/)) {
pontos++;
}

if (string.match(/[A-Z]/)) {
pontos++;
}

return pontos;
}
