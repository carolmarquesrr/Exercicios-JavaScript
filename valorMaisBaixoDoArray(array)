function valorMaisBaixoDoArray(array){
    let valorMaisBaixo = array[0];

    for(let i = 1; i < array.length; i++){
        if(array[i] < valorMaisBaixo){
            valorMaisBaixo = array[i];
        }
    }
    return valorMaisBaixo;
}
