function somatorio(array){
    // escreve aqui a função
    let initial= 0;
    let soma = array.reduce((acc,cur) => acc + cur, initial);
return soma;
}

function somatorio(array) {
  return array.reduce((acc, cur) => acc + cur, 0);
}

