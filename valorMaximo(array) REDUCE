function valorMaximo(array){
    // encontrar o valor maximo do array usando reduce

    let valorM = array.reduce((max, current) =>{
        return Math.max(max, current);
    }, 0);
    return valorM;
}
