function contaNumeroDeDivisoesPorDois(n) {
  let contador = 0;
  while (n >= 1) {
    numero = n / 2;
    contador++;
  }
  return contador - 1; // subtrai 1 para descontar a última divisão que resulta em um número menor que 1
}

console.log(contaNumeroDeDivisoesPorDois(9)); // output deve ser 3






Cria uma função que recebe como argumento um número e retorna
o número de vezes em que é possível fazer a divisão inteira desse número por 2.
Utiliza o que aprendeste sobre FOR ou WHILE para resolver este exercício.

function contaNumeroDeDivisoesPorDois(n) {
  let count = 0;

  while (n >= 2) {
    n = Math.floor(n / 2);
    count++;
  }

  return count;
}

console.log(contaNumeroDeDivisoesPorDois(9)); // 3
console.log(contaNumeroDeDivisoesPorDois(16)); // 4
console.log(contaNumeroDeDivisoesPorDois(5)); // 2
