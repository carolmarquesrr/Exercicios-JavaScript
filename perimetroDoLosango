Cria uma função que recebe duas diagonais de 
    um losango como argumentos e calcula o perímetro
     dessa figura geométrica.

Utiliza o que aprendeste sobre  number para resolver este exercício.


function perimetroDoLosango(diagonalMaior, diagonalMenor) {
    let lado = Math.sqrt((diagonalMaior/2)**2 + (diagonalMenor/2)**2);
    let perimetroDoLosango = lado * 4;
    return perimetroDoLosango;
}

